name: papercraft_systems
description: PaperCraft Systems — versión Flutter, conectada a la misma base de datos Supabase que la web.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  intl: ^0.19.0
  crypto: ^3.0.3
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
